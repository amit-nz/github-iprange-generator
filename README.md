# github-iprange-generator
Python script to generates a pair of files containing both IPv4 &amp; IPv6 github endpoints.

Just run

`$  python3 github-2-cloudflarelist.py`

And it spits out two text files- `GitHub_IP4_YYYYMMDD.txt` & `GitHub_IP6_YYYYMMDD.txt` for you to do with as you please.
